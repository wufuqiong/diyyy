# 待处理需求

## 已完成

- hundred-chart：百数板填空和拼图填空中去掉页面标题黑色小标题，输入框改成带 InputLabel 的 ✅
- charmaze：选择全册所有词语后渲染 crash → 已修复（`MAX_PAGES = 50`，优化 `loadMiemieLessons` 缓存和填充池预计算）
- chartrace：配置页改为 SettingCard 风格（白色卡片 + 绿色主题色 + 标题分块）✅
- chartrace：InputLabel 替换 SettingsField 小标题（Select/TextField 控件）✅
- charmaze：配置页改为 chartrace 风格教材选择（级别 + 课册多选 + 模式按钮）✅
- charcolor：书册选择器改为多选 ✅
- 侧边栏图标与标题栏统一 ✅
- math-genie：多步运算和填空字体自适应缩放 ✅

## 待处理

_暂无_
