# Math Genie 比较练习现状信息收集 Prompt

## 背景

之前基于截图给出了 math-genie "比较练习"功能的信息架构重设计建议
（两步式：先选题型大类，再展开详细配置），过程中发现至少一处判断
是基于猜测、且被你纠正过（"比较练习也有应用题"，揭示了
`WORD_PROBLEM_COMPARISON` 这个现有功能实际上属于比较练习，而不是
加减法练习的专项练习）。

为避免继续在不了解实际代码现状的情况下做结构性判断，请提供以下
信息，帮助后续的 design doc 和原型设计建立在真实代码现状上，而非
截图推测。

---

## 1. 当前 `SpecialPracticeType` 与比较练习相关的实际定义

请提供：

1. 当前 `SpecialPracticeType` 枚举的完整定义（包括所有值，不仅是
   和比较练习相关的），以及每个值在 `config.tsx` 或相关类型文件中
   的准确名称
2. "比较练习"（截图中"专项练习"五个选项的最后一个）当前的枚举值
   叫什么名字？它和设计文档中提到的 `WORD_PROBLEM_COMPARISON` 是
   同一个值，还是这次新增的一个独立值？
3. 截图中"比较题型"下的"比大小"/"比多少"两个选项，对应的类型定义
   是什么（是否已有类似 `ComparisonSubType` 这样的枚举，还是用
   别的字段名表示）？

---

## 2. 比较练习当前支持的呈现方式（DisplayMode）覆盖情况

请逐一确认，比较练习（无论是"比大小"还是"比多少"）当前是否已经
支持以下三种呈现方式，每种请说明实现状态：

| 呈现方式 | 是否已实现 | 对应的生成函数/组件（如果已实现） |
|---|---|---|
| 纯文本/数字（如图2的 `8○3` 形式） | ? | ? |
| 图案对比（如图1的 emoji 数量对比） | ? | ? |
| 应用题（文字描述场景） | ? | ? |

**特别说明应用题部分**：请确认当前"应用题"形式的比较题，实际题目
内容长什么样（请提供 1-2 个真实生成结果的文字示例，而非概括描述），
以及它在代码里具体是复用了原有的 `WORD_PROBLEM_COMPARISON` 逻辑，
还是有独立的生成函数。

---

## 3. "比大小" vs "比多少" 两个子题型的实际区别

请说明这两个子题型在生成逻辑上的具体区别：

- "比大小"是否就是设计文档中提到的纯数字 `>`/`<`/`=` 判断（对应
  参考图2），还是也支持图案形式？
- "比多少"是否对应参考图1中"圈多/少"或"填空差值"这类题型？还是
  指的是别的逻辑？
- 这两个子题型是否分别都能在"纯数字/图案/应用题"三种呈现方式下
  生成，还是某些组合当前不支持（例如"比多少+纯数字"是否有意义，
  还是这个组合本身在产品逻辑上不存在）？

---

## 4. 当前 UI 实现中，"难度模式"（单一/混合）和"数值范围"对比较练习的实际影响

截图中"单一/混合"tab 和"1-5/1-10/1-20/自定义"范围选择器，在
加减法练习中已有对应逻辑（参考设计文档中 `difficultyRatios` 和
`DifficultyLevel`）。请确认：

- 比较练习选中后，这两个控件是否复用了和加减法练习完全相同的底层
  状态字段（即 `difficulty`/`difficultyRatios`），还是比较练习有
  自己独立的一套数值范围配置？
- 如果是复用同一套字段，切换"题型大类"（加减法 ↔ 比较）时，
  这个数值范围设置是否会被保留，还是重置？

---

## 5. 当前代码中，"图案"呈现方式使用的 emoji 池

请提供当前 `DisplayMode.EMOJI` 模式实际使用的表情符号集合（完整
列表或所在文件路径），以及比较练习的"图案对比"呈现方式是否复用
了同一份 emoji 池，还是有自己单独定义的图案集合（截图中出现了
苹果、蓝莓、葡萄、芒果、香蕉、草莓、西瓜、桃子等多种水果图案，
请确认这些具体来自哪里）。

---

## 6. 现有"专项练习"五个选项之间的实际互斥/依赖关系

请提供当前代码中，"无/零/数组家族/数的分合/比较练习"这五个选项
被选中后，对其他配置字段（运算类型、题目类型、显示模式等）的
实际启用/禁用逻辑——最好是列出代码中实际的条件判断逻辑摘要，
而非仅描述 UI 表现。这是为了确认重新设计两步式架构后，需要保留
哪些现有的互斥规则，避免重构时遗漏。

---

## 输出要求

- 请基于实际代码内容回答，不要基于截图或推测概括
- 如果某一项当前确实尚未实现（比如某个呈现方式组合目前不存在），
  请明确说"未实现"，不要为了回答完整而编造
- 涉及生成逻辑的部分，欢迎直接贴出关键函数签名或核心代码片段，
  比概括性文字描述更有用
